* Export command
  Implement an export command that would generate a zip file
  containing the report results, the original report, and the policy
  used for evaluation.

* External parser support
  Allow report parsers to be invoked through a simple external API so
  that new report types can be added without modifying the Lisp code.
